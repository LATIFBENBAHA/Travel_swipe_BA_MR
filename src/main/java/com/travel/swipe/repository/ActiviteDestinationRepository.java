package com.travel.swipe.repository;

import com.travel.swipe.model.ActiviteDestination;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ActiviteDestinationRepository extends JpaRepository<ActiviteDestination, Long> {
}
